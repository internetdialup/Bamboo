# Bamboo OS Dependencies
# These are required for the runtime governance engine.
# The core discipline (behavior/) is dependency-free.

psutil>=5.9.0
pyyaml>=6.0
numpy>=1.24.0
sentence-transformers>=2.2.2
