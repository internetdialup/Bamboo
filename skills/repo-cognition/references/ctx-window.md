# ctx-window — pointer

This file is a pointer. The canonical content lives at [`behavior/ctx-window.md`](../../../behavior/ctx-window.md).

The `repo-cognition` Skill used to mirror the four `behavior/ctx-*.md` docs into this `references/` folder so the Skill loader could resolve them by relative path. That mirror layer was Drift fuel — the exact pattern `behavior/ctx-entropy.md` warns against. The Skill and its vendor overlays now load canonical paths directly; this stub stays so any older link still resolves.

Canonical: [`behavior/ctx-window.md`](../../../behavior/ctx-window.md)
